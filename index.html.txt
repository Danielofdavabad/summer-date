<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>دعوت به قرار تابستونی | Kimia</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&family=Pacifico&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            user-select: none;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(145deg, #f9a826 0%, #fddb6a 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }

        /* =============== خورشید جدید با جزئیات طلایی =============== */
        .sun {
            position: absolute;
            top: 20px;
            left: 20px;
            width: 110px;
            height: 110px;
            background: radial-gradient(circle, #FFD966, #FFB347);
            border-radius: 50%;
            box-shadow: 0 0 40px #ffaa33, 0 0 20px #ffdd88 inset;
            z-index: 0;
            animation: sunGlow 3s infinite alternate;
        }
        .sun::before, .sun::after {
            content: '';
            position: absolute;
            background: radial-gradient(circle, #FFD700, #FFA500);
            border-radius: 50%;
            opacity: 0.7;
        }
        .sun::before {
            width: 140%;
            height: 140%;
            top: -20%;
            left: -20%;
            z-index: -1;
            filter: blur(8px);
        }
        .sun::after {
            width: 160%;
            height: 160%;
            top: -30%;
            left: -30%;
            z-index: -2;
            filter: blur(15px);
            opacity: 0.4;
        }
        /* پرتوهای خورشید */
        .sun-rays {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 160px;
            height: 160px;
            transform: translate(-50%, -50%);
            background: repeating-conic-gradient(from 0deg, #FFE484 0deg 10deg, transparent 10deg 20deg);
            border-radius: 50%;
            z-index: -1;
            animation: rotateRays 20s linear infinite;
        }
        @keyframes sunGlow {
            0% { box-shadow: 0 0 30px #ffaa33, 0 0 15px inset #ffdd88; }
            100% { box-shadow: 0 0 70px #ffcc44, 0 0 30px inset #ffffaa; }
        }
        @keyframes rotateRays {
            from { transform: translate(-50%, -50%) rotate(0deg); }
            to { transform: translate(-50%, -50%) rotate(360deg); }
        }

        .cloud {
            position: absolute;
            background: white;
            border-radius: 1000px;
            opacity: 0.8;
            z-index: 0;
        }
        .cloud1 { width: 120px; height: 70px; top: 80px; right: 20px; }
        .cloud1:before, .cloud1:after {
            content: '';
            position: absolute;
            background: white;
            border-radius: 50%;
        }
        .cloud1:before { width: 50px; height: 50px; top: -25px; left: 15px; }
        .cloud1:after { width: 70px; height: 70px; top: -35px; left: 45px; }

        .cloud2 { width: 180px; height: 90px; bottom: 100px; left: -50px; }
        .cloud2:before, .cloud2:after {
            content: '';
            position: absolute;
            background: white;
            border-radius: 50%;
        }
        .cloud2:before { width: 70px; height: 70px; top: -30px; left: 20px; }
        .cloud2:after { width: 90px; height: 90px; top: -45px; left: 60px; }

        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(8px);
            border-radius: 60px 60px 80px 80px;
            box-shadow: 0 30px 40px rgba(0,0,0,0.2);
            padding: 40px 30px 50px;
            max-width: 650px;
            width: 100%;
            text-align: center;
            z-index: 2;
            transition: all 0.3s ease;
            border: 2px solid rgba(255,245,200,0.8);
        }

        /* کادر خوشگل برای Kimia با گل رز */
        .kimia-box {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            background: rgba(255, 220, 200, 0.7);
            backdrop-filter: blur(4px);
            padding: 12px 28px;
            border-radius: 100px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1), inset 0 1px 0 rgba(255,255,255,0.8);
            border: 1px solid #ffb6c1;
            margin-bottom: 20px;
        }
        .kimia-name {
            font-family: 'Dancing Script', 'Pacifico', cursive;
            font-size: 3rem;
            font-weight: bold;
            color: #9b59b6;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
            letter-spacing: 2px;
        }
        .rose {
            font-size: 2.2rem;
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
        }

        .question {
            font-size: 1.8rem;
            font-weight: bold;
            background: #fff0d4;
            padding: 20px;
            border-radius: 50px;
            margin: 20px 0 35px;
            color: #c0392b;
            box-shadow: inset 0 -2px 0 #f0c0a0, 0 10px 15px -5px rgba(0,0,0,0.1);
        }

        .buttons {
            display: flex;
            justify-content: center;
            gap: 40px;
            flex-wrap: wrap;
            position: relative;
            min-height: 140px;
        }

        .btn {
            font-size: 1.8rem;
            font-weight: bold;
            padding: 12px 32px;
            border: none;
            border-radius: 60px;
            cursor: pointer;
            transition: 0.2s;
            box-shadow: 0 5px 0 rgba(0,0,0,0.2);
            font-family: inherit;
        }

        .btn-yes {
            background-color: #2ecc71;
            color: white;
            text-shadow: 0 1px 1px green;
            position: relative;
            z-index: 10;
        }
        .btn-yes:hover {
            background-color: #27ae60;
            transform: scale(1.03);
        }

        .btn-no {
            background-color: #e74c3c;
            color: white;
            text-shadow: 0 1px 1px #8b0000;
            position: relative;
            transition: all 0.15s ease;
            z-index: 5;
        }

        .move-away {
            transition: top 0.2s ease, left 0.2s ease, transform 0.15s ease;
        }

        /* پنل مرحله دوم */
        .second-panel {
            margin-top: 30px;
            background: rgba(255, 250, 225, 0.95);
            border-radius: 50px;
            padding: 25px;
            text-align: right;
            display: none;
            border: 2px solid #ffcc80;
            box-shadow: 0 15px 25px rgba(0,0,0,0.1);
            animation: fadeSlideUp 0.5s ease;
        }

        @keyframes fadeSlideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .form-group {
            margin-bottom: 20px;
            text-align: right;
        }

        .form-group label {
            display: block;
            font-weight: bold;
            color: #b64926;
            margin-bottom: 8px;
            font-size: 1.1rem;
        }

        .form-group select, .form-group input {
            width: 100%;
            padding: 12px 15px;
            border-radius: 40px;
            border: 1px solid #ffb347;
            background: #fffef7;
            font-size: 1rem;
            font-family: inherit;
            outline: none;
            transition: 0.2s;
        }

        .date-row {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .date-row select {
            flex: 1;
        }

        .radio-group {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            justify-content: flex-start;
            margin-top: 8px;
        }
        .radio-group label {
            font-weight: normal;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: #fff3e0;
            padding: 6px 15px;
            border-radius: 40px;
            cursor: pointer;
        }
        .radio-group input {
            width: auto;
            margin: 0;
        }

        .submit-plan {
            background: #e67e22;
            color: white;
            font-size: 1.3rem;
            padding: 12px 25px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 15px;
            width: 100%;
            transition: 0.2s;
        }
        .submit-plan:hover {
            background: #d35400;
            transform: scale(1.02);
        }

        .success-message {
            margin-top: 20px;
            background: #2ecc71;
            color: white;
            padding: 15px;
            border-radius: 40px;
            display: none;
            animation: bounce 0.5s;
        }

        @keyframes bounce {
            0% { transform: scale(0.9); opacity: 0; }
            80% { transform: scale(1.02); }
            100% { transform: scale(1); opacity: 1; }
        }

        footer {
            margin-top: 25px;
            font-size: 0.9rem;
            color: #b85c1a;
            font-weight: 500;
        }

        .flower {
            position: absolute;
            bottom: 15px;
            left: 15px;
            font-size: 2rem;
            opacity: 0.6;
            z-index: 1;
        }

        @media (max-width: 550px) {
            .card { padding: 25px 20px; }
            .kimia-name { font-size: 2.4rem; }
            .rose { font-size: 1.8rem; }
            .question { font-size: 1.4rem; }
            .btn { font-size: 1.4rem; padding: 8px 24px; }
            .buttons { gap: 20px; }
            .sun { width: 70px; height: 70px; }
        }
    </style>
</head>
<body>
<div class="sun">
    <div class="sun-rays"></div>
</div>
<div class="cloud cloud1"></div>
<div class="cloud cloud2"></div>
<div class="flower">🌸🍉🌞🍦</div>

<div class="card" id="mainCard">
    <!-- کادر خوشگل برای Kimia با رز قرمز -->
    <div class="kimia-box">
        <span class="rose">🌹</span>
        <span class="kimia-name">Kimia</span>
        <span class="rose">🌹</span>
        <span class="rose">🌹</span>
    </div>
    
    <!-- حذف شد "یه پیشنهاد خنک" -->
    
    <div class="question" id="questionDiv">
        با من میای سرِ قرار تابستونی؟
    </div>
    <div class="buttons" id="initialButtons">
        <button class="btn btn-yes" id="yesBtn">🗸 بله، حتماً!</button>
        <button class="btn btn-no" id="noBtn">✗ نه</button>
    </div>
    
    <!-- پنل مرحله دوم -->
    <div id="planPanel" class="second-panel">
        <h3 style="color:#c0392b; margin-bottom:20px;">🌸 جزئیات قرار رو بگو 🌸</h3>
        
        <div class="form-group">
            <label>📅 تاریخ قرار (فقط تابستان ۱۴۰۵):</label>
            <div class="date-row">
                <select id="yearSelect"></select>
                <select id="monthSelect"></select>
                <select id="daySelect"></select>
            </div>
        </div>
        
        <div class="form-group">
            <label>⏰ ساعت قرار:</label>
            <input type="time" id="timeInput" value="19:00">
        </div>
        
        <div class="form-group">
            <label>🍽️ چه خوراکی دوست داری؟</label>
            <div class="radio-group">
                <label><input type="radio" name="meal" value="صبحانه"> صبحانه</label>
                <label><input type="radio" name="meal" value="ناهار"> ناهار</label>
                <label><input type="radio" name="meal" value="شام" checked> شام</label>
                <label><input type="radio" name="meal" value="عصرونه"> عصرونه</label>
            </div>
        </div>
        
        <div class="form-group">
            <label>🌹 گل مورد علاقه‌ات (از بازار ایران):</label>
            <select id="flowerSelect">
                <option value="رز سرخ">🌹 رز سرخ</option>
                <option value="لاله">🌷 لاله</option>
                <option value="میخک">🌸 میخک</option>
                <option value="نرگس">🌼 نرگس</option>
                <option value="یاس">🤍 یاس</option>
                <option value="مریم">🤍 مریم</option>
                <option value="گل محمدی">🌹 گل محمدی</option>
                <option value="سنبل">💜 سنبل</option>
                <option value="زنبق">💙 زنبق</option>
                <option value="آفتابگردان">🌻 آفتابگردان</option>
            </select>
        </div>
        
        <button id="submitPlanBtn" class="submit-plan">✨ ثبت قرار عاشقانه ✨</button>
        <div id="finalSuccess" class="success-message"></div>
    </div>
    
    <!-- متن پایین جدید (قابل تغییر) -->
    <footer>🍃 منتظر یه قرار خاطره‌انگیز توی گرمای تابستونم... بیا تا با هم خورشید رو تماشا کنیم 🌅</footer>
</div>

<script>
    (function() {
        // ------------------- فرار دکمه نه بدون همپوشانی با دکمه بله -------------------
        const noBtn = document.getElementById('noBtn');
        const yesBtn = document.getElementById('yesBtn');
        const initialButtons = document.getElementById('initialButtons');
        const questionDiv = document.getElementById('questionDiv');
        const planPanel = document.getElementById('planPanel');
        
        // تابع تشخیص همپوشانی دو دکمه
        function isOverlap(noRect, yesRect) {
            return !(noRect.right < yesRect.left || 
                     noRect.left > yesRect.right || 
                     noRect.bottom < yesRect.top || 
                     noRect.top > yesRect.bottom);
        }
        
        function moveNoButton() {
            if (!noBtn) return;
            const containerRect = initialButtons.getBoundingClientRect();
            const btnRect = noBtn.getBoundingClientRect();
            const yesRect = yesBtn.getBoundingClientRect();
            
            // حاشیه امن برای حرکت
            const margin = 10;
            const maxLeft = containerRect.width - btnRect.width - margin;
            const maxTop = containerRect.height - btnRect.height - margin;
            let attempts = 0;
            let newLeft, newTop, noRectTest;
            
            do {
                newLeft = Math.random() * maxLeft;
                newTop = Math.random() * maxTop;
                // ساخت المان مجازی برای تست
                noRectTest = {
                    left: containerRect.left + newLeft,
                    right: containerRect.left + newLeft + btnRect.width,
                    top: containerRect.top + newTop,
                    bottom: containerRect.top + newTop + btnRect.height
                };
                attempts++;
                if (attempts > 50) break; // جلوگیری از حلقه بی‌نهایت
            } while (isOverlap(noRectTest, yesRect));
            
            noBtn.style.position = 'absolute';
            noBtn.style.left = newLeft + 'px';
            noBtn.style.top = newTop + 'px';
            noBtn.style.margin = '0';
            noBtn.classList.add('move-away');
        }
        
        // رویدادها برای فرار نه
        noBtn.addEventListener('mouseenter', moveNoButton);
        noBtn.addEventListener('touchstart', function(e) {
            e.preventDefault();
            moveNoButton();
        });
        noBtn.addEventListener('click', function(e) {
            e.preventDefault();
            moveNoButton();
            noBtn.style.transform = 'rotate(2deg) scale(0.98)';
            setTimeout(() => { noBtn.style.transform = ''; }, 150);
        });
        noBtn.addEventListener('focus', function() { noBtn.blur(); moveNoButton(); });
        
        // وقتی بله زده شد
        yesBtn.addEventListener('click', function() {
            initialButtons.style.display = 'none';
            questionDiv.style.display = 'none';
            planPanel.style.display = 'block';
            populateSummerDates(); // فقط تابستان 1405
            tinyConfetti();
        });
        
        // ------------------- تاریخ فقط تابستان 1405 (تیر، مرداد، شهریور) -------------------
        function populateSummerDates() {
            const yearSelect = document.getElementById('yearSelect');
            const monthSelect = document.getElementById('monthSelect');
            const daySelect = document.getElementById('daySelect');
            
            // فقط سال 1405
            yearSelect.innerHTML = '';
            let year = 1405;
            let optionYear = document.createElement('option');
            optionYear.value = year;
            optionYear.textContent = year;
            optionYear.selected = true;
            yearSelect.appendChild(optionYear);
            
            // ماه‌های تابستان: 4=تیر، 5=مرداد، 6=شهریور
            const summerMonths = [
                { value: 4, name: 'تیر' },
                { value: 5, name: 'مرداد' },
                { value: 6, name: 'شهریور' }
            ];
            monthSelect.innerHTML = '';
            summerMonths.forEach(m => {
                let opt = document.createElement('option');
                opt.value = m.value;
                opt.textContent = `${m.value} - ${m.name}`;
                if (m.value === 4) opt.selected = true;
                monthSelect.appendChild(opt);
            });
            
            function updateDays() {
                let month = parseInt(monthSelect.value);
                let daysInMonth = (month <= 6) ? 31 : 30; // ماه‌های اول تا ششم 31 روز دارند
                daySelect.innerHTML = '';
                for (let i = 1; i <= daysInMonth; i++) {
                    let opt = document.createElement('option');
                    opt.value = i;
                    opt.textContent = i;
                    if (i === 10) opt.selected = true;
                    daySelect.appendChild(opt);
                }
            }
            monthSelect.addEventListener('change', updateDays);
            updateDays();
        }
        
        // ------------------- ثبت نهایی (با ذخیره در localStorage برای اینکه بعداً ببینی) -------------------
        const submitBtn = document.getElementById('submitPlanBtn');
        const finalSuccessDiv = document.getElementById('finalSuccess');
        
        submitBtn.addEventListener('click', function() {
            const year = document.getElementById('yearSelect').value;
            const month = parseInt(document.getElementById('monthSelect').value);
            const monthName = getMonthName(month);
            const day = document.getElementById('daySelect').value;
            const time = document.getElementById('timeInput').value;
            const meal = document.querySelector('input[name="meal"]:checked').value;
            const flowerSelect = document.getElementById('flowerSelect');
            const flower = flowerSelect.options[flowerSelect.selectedIndex].text;
            
            const shamsiDate = `${year}/${month}/${day} (${monthName})`;
            const message = `💖 عالی شد! قرار ما در تاریخ ${shamsiDate} ساعت ${time}، همراه با ${meal} و گل ${flower} رزرو شد. منتظر تو هستم تا خاطره‌ای قشنگ بسازیم 💖`;
            finalSuccessDiv.innerHTML = message;
            finalSuccessDiv.style.display = 'block';
            finalSuccessDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            
            // ذخیره اطلاعات در localStorage تا بعداً خودت (طراح) بتونی ببینی
            const selectedData = {
                date: shamsiDate,
                time: time,
                meal: meal,
                flower: flower,
                timestamp: new Date().toISOString()
            };
            localStorage.setItem('Kimia_DateChoice', JSON.stringify(selectedData));
            // نمایش در کنسول برای تست (تو می‌تونی بعداً با F12 کنسول رو ببینی)
            console.log('اطلاعات انتخاب شده:', selectedData);
            
            fullConfetti();
            submitBtn.disabled = true;
            submitBtn.style.opacity = '0.6';
            submitBtn.textContent = '❤️ قرار ثبت شد ❤️';
            
            // یه پیام اضافه برای طراح: چطور اطلاعات رو ببینه
            let infoMsg = document.createElement('div');
            infoMsg.style.marginTop = '15px';
            infoMsg.style.fontSize = '0.8rem';
            infoMsg.style.color = '#2c3e50';
            infoMsg.style.background = '#fff1c1';
            infoMsg.style.padding = '8px';
            infoMsg.style.borderRadius = '30px';
            infoMsg.innerHTML = '🔒 اطلاعات قرار در مرورگر ذخیره شد. برای دیدن، کلید F12 را بزنید و به بخش Console بروید.';
            finalSuccessDiv.parentNode.insertBefore(infoMsg, finalSuccessDiv.nextSibling);
        });
        
        function getMonthName(m) {
            const names = ['', 'فروردین', 'اردیبهشت', 'خرداد', 'تیر', 'مرداد', 'شهریور', 'مهر', 'آبان', 'آذر', 'دی', 'بهمن', 'اسفند'];
            return names[m];
        }
        
        // ------------------- افکت‌ها -------------------
        function tinyConfetti() {
            for(let i=0;i<40;i++) {
                let emoji = ['🎉','✨','🍉','🌸','⭐','💖','🍦'][Math.floor(Math.random()*7)];
                let div = document.createElement('div');
                div.innerText = emoji;
                div.style.position = 'fixed';
                div.style.left = Math.random() * window.innerWidth + 'px';
                div.style.top = '-20px';
                div.style.fontSize = (15+Math.random()*25)+'px';
                div.style.pointerEvents = 'none';
                div.style.zIndex = '10000';
                div.style.transition = 'top 1.2s ease-in, opacity 0.8s';
                document.body.appendChild(div);
                setTimeout(() => { div.style.top = window.innerHeight + 'px'; div.style.opacity = '0'; }, 10);
                setTimeout(() => { div.remove(); }, 1500);
            }
        }
        
        function fullConfetti() {
            for(let i=0;i<120;i++) {
                let emoji = ['🎉','🌸','🌼','🍉','🍧','💝','✨','⭐','🌹','💐'][Math.floor(Math.random()*10)];
                let div = document.createElement('div');
                div.innerText = emoji;
                div.style.position = 'fixed';
                div.style.left = Math.random() * window.innerWidth + 'px';
                div.style.top = '-30px';
                div.style.fontSize = (18+Math.random()*35)+'px';
                div.style.pointerEvents = 'none';
                div.style.zIndex = '10001';
                div.style.transition = 'top 1.8s ease-out, opacity 1.2s';
                div.style.opacity = '0.9';
                document.body.appendChild(div);
                setTimeout(() => { div.style.top = window.innerHeight + 'px'; div.style.opacity = '0'; }, 20);
                setTimeout(() => { div.remove(); }, 2200);
            }
        }
        
        // ریسایز برای دکمه نه
        window.addEventListener('resize', function() {
            if(noBtn && noBtn.style.position === 'absolute') {
                const containerRect = initialButtons.getBoundingClientRect();
                const btnRect = noBtn.getBoundingClientRect();
                let leftVal = parseFloat(noBtn.style.left);
                let topVal = parseFloat(noBtn.style.top);
                if (isNaN(leftVal)) leftVal = 0;
                if (isNaN(topVal)) topVal = 0;
                const maxLeft = containerRect.width - btnRect.width - 10;
                const maxTop = containerRect.height - btnRect.height - 10;
                if(leftVal > maxLeft) noBtn.style.left = Math.max(0, maxLeft) + 'px';
                if(topVal > maxTop) noBtn.style.top = Math.max(0, maxTop) + 'px';
                // جلوگیری از همپوشانی مجدد بعد از ریسایز
                const yesRect = yesBtn.getBoundingClientRect();
                const noRect = noBtn.getBoundingClientRect();
                if (isOverlap(noRect, yesRect)) {
                    moveNoButton();
                }
            }
        });
        
        // تعریف تابع isOverlap در دسترس گلوبال
        window.isOverlap = isOverlap;
    })();
</script>
</body>
</html>